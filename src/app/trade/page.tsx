import { TradeDashboard } from "@/components/wine/trade/TradeDashboard";

export const metadata = { title: "Négoce — Wine Signals" };

export default function TradePage() {
  return <TradeDashboard />;
}
